export enum RoutesEnum {
  Home = "/",
  Films = "/films",
  Series = "/series",
  Cartoons = "/cartoons",
  Cabinet = "/cabinet",
  NewFilms = "/new-films",
  ComedyFilms = "/comedy-films",
  ForFamily = "/for-family",
}

export const LIMIT = 15;
